import { EtrackUserCanDirective } from './etrack-user-can.directive';

describe('EtrackUserCanDirective', () => {
  it('should create an instance', () => {
    const directive = new EtrackUserCanDirective();
    expect(directive).toBeTruthy();
  });
});
